{
  "name": "user-migration-script",
  "version": "1.0.0",
  "description": "Script to migrate users from old database to new schema",
  "main": "migrate-users.ts",
  "scripts": {
    "migrate": "ts-node migrate-users.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.3.1",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "@types/node": "^20.8.0"
  }
}
